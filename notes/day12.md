# Day 12 学習記録

## 今日やったこと

- pandasを使ってCSVファイルを読み込んだ
- `pd.read_csv()` で `etf_prices.csv` を読み込んだ
- `print(df)` で表全体を表示した
- `df["価格"]` で価格の列だけを取り出した
- `df.head(2)` で先頭2行だけを表示した

## 今日わかったこと

- pandasを使うと、CSVを表のように扱える
- `DataFrame` は表形式のデータを入れるもの
- `read_csv()` はCSVファイルを読み込むために使う
- `head()` は先頭の数行だけ確認するときに使う

## 詰まったところ

- ファイル操作系は、テキスト、CSV、JSON、pandasで使うコマンドが違うので覚えるのが大変
- PowerShellで日本語が文字化けして見えることがあった

## 自分の言葉で説明

- pandasは、CSVのような表データを読み込んで、行や列を確認しやすくするために使う
- ファイル形式によって読み書きの方法が違うので、何の形式を扱っているかを意識する必要がある

## 次回やること

- FastAPIの入門を学ぶ
