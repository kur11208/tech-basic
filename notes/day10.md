# Day 10 学習記録

## 今日やったこと

- JSONファイルにETF名と価格を書き込んだ
- JSONファイルを読み込んで、ETF名と価格を表示した
- `json.dump()` と `json.load()` を使った

## 今日わかったこと

- JSONファイルの扱い方
- ファイルの扱い方と表示の仕方
- `dict` のデータをJSONファイルに保存できること

## 詰まったところ

- `print(file)` ではファイルの中身ではなく、ファイルそのものの情報が表示されること
- 同じキーを2回使うと、後ろの値で上書きされること

## 自分の言葉で説明

- JSONは、Pythonの`dict`のようなデータをファイルに保存して、あとから読み込むために使う

## 次回やること

- Day 11で`datetime`を使って日付と時刻を扱う
