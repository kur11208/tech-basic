# Day 15 学習記録

## 今日やったこと

- FastAPIでHTMLを表示する処理を書いた
- `HTMLResponse` を使ってHTMLとして返す方法を確認した
- `/` にアクセスしたときに見出しと文章を表示した
- 存在しないURLでは `404 Not Found` になることを確認した

## 今日わかったこと

- FastAPIでHTMLを表示できる
- HTMLはブラウザに見出しや文章を表示するために使う
- サーバー起動コマンドの `day15:app` は、`day15.py` の中の `app` を起動するという意味

## 詰まったところ

- サーバーの起動コマンドがまだ覚えられない

## 自分の言葉で説明

- HTMLは、ブラウザに見出しや文章を表示するためのもの
- FastAPIでは `HTMLResponse` を使うと、HTMLとして表示できる

## 次回やること

- HTMLの画面を少し見やすくする
